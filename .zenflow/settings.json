{
  "setup_script": "cargo build --all-targets --all-features",
  "verification_script": "cargo fmt --all --check && cargo clippy --all-targets --all-features -- -W clippy::all && cargo test --all-features"
}
